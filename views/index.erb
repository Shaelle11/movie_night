<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lady Whistledown's Invitation</title>
    <link rel="stylesheet" href="/styles.css">
     <style>
        .overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.5);
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .popup {
            background: white;
            padding: 20px;
            border-radius: 10px;
            text-align: center;
            max-width: 500px;
            width: 90%;
        }

       .pop {
            height: 50px;
            margin: 0 10px;
        }

        .field {
            margin-bottom: 15px;
        }

        .error {
            color: red;
        }
    </style>
</head>
<body>
    <div id="overlay" class="overlay">
        <div class="popup">
            <div class="header">
                <img src="/images/LADYW.png" alt="Image Before Hello" class="pop">
                <h1>Hello,</h1>
                <img src="/images/TUC.png" alt="Image After Hello" class="pop">
            </div>
            <% if @error %>
              <p class="error"><%= @error %></p>
            <% end %>
            <form action="/invite" method="post">
                <div class="field">
                    <label for="title">Title:</label>
                    <select id="title" name="title" required>
                        <option value="Lord">Lord</option>
                        <option value="Lady">Lady</option>
                    </select>
                </div>
                <div class="field">
                    <label for="name">Name:</label>
                    <input type="text" id="name" name="name" required>
                </div>
                <div class="field">
                    <label for="passkey">PassCode:</label>
                    <input type="password" id="passkey" name="passkey" required>
                </div>
                <button type="submit">Submit</button>
            </form>
        </div>
    </div>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            document.getElementById('overlay').style.display = 'block';
        });
    </script>
</body>
</html>